var WaterBottle = function() {
  this.volume = 0
  // Initialise volume as zero.
}

module.exports = WaterBottle
// This wraps up the WaterBottle and puts it into module.exports. WaterBottle is what is being exposed to the test file.